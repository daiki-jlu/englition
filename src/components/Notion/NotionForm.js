import React from "react";

function NotionForm() {
    return (
        <form>
            <input type="text" placeholder="Enter text to save to Notion" />
            <button>Save</button>
        </form>
    );
}

export default NotionForm;
